version: 1

deploy:
  steps:
    setupVariables:
      after: &login-k8s
        - echo 'ma'
        - echo 'luz'
        - mkdir -p ~/.kube
        - aws eks --region=us-east-1 update-kubeconfig --name kushield --alias kushield
        - kubectl get namespaces

destroy:
  steps:
    setupVariables:
      after: *login-k8s
