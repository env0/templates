version: 1

deploy:
  steps:
    terraformApply:
      after:
        - |
            export VM_IP=$(terraform output vm_default_ip_address)
            ./pass.sh $SSH_PASS ssh -o "StrictHostKeyChecking no" env0@$VM_IP
