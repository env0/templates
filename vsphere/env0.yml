version: 1

deploy:
  steps:
    terraformApply:
      after:
        - |
            pip install paramiko
            export VM_IP=$(terraform output vm_default_ip_address)
            python ssh.py            
