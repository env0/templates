version: 2
deploy:
  steps:
    terraformInit:
      before:
        - echo $ENV0_OIDC_TOKEN > web-identity-token.txt
        - echo Replacing !!!USER!!! with $USER in index.html
        - sed 's/!!!USER!!!/'"$USER"'/g' index.template.html > index.html
