version: 1

deploy:
  steps:
    setupVariables:
      after:
        - printenv
    helmDiff:
      before:
        - printenv
      after:
        - printenv
    helmUpgrade:
      before:
        - printenv
      after:
        - printenv
    terraformPlan:
      before:
        - printenv
      after:
        - printenv

    terraformApply:
      before:
        - printenv
      after:
        - printenv

destroy:
  steps:
    setupVariables:
      after:
        - printenv
    helmDiff:
      before:
        - printenv
      after:
        - printenv
    helmUninstall:
      before:
        - printenv
      after:
        - printenv