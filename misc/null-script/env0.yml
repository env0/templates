version: 1

deploy:
  steps:
    setupVariables:
      after:
        - whoami
        - id node
        - ls -al /mnt
        - ls -al /mnt/private/env0
    terraformOutput:
      after:
        - printenv
