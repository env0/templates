version: 1

deploy:
  steps:
    setupVariables:
      after:
        - whoami
        - id node
        - ls -al /mnt
        - ls -al /mnt/private/env0/shag-dev-two
    terraformOutput:
      after:
        - printenv
