version: 1

deploy:
  steps:
    setupVariables:
      after:
        - whoami
        - id node
        - ls -al /mnt
        - ls -al /mnt/env0/kushield
    terraformOutput:
      after:
        - printenv
