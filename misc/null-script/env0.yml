version: 1

deploy:
  steps:
    setupVariables:
      after:
        - whoami
        - id node
        - echo "hello world" >> helloWorld.txt
        - cat helloWorld.txt
        - ls
        - ls -al ..
        - ls -al ../..
        - ls -al ../../..
        - ls -al /mnt/private
    terraformOutput:
      after:
        - printenv
