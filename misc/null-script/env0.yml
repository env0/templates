version: 1

deploy:
  steps:
    setupVariables:
      after:
        - whoami
        - id node
        - echo "hello world" >> helloWorld.txt
        - cat helloWorld.txt
        - ls
        - ls -al ..
        - ls -al ../..
        - ls -al ../../..
        - cat /proc/mounts
    terraformOutput:
      after:
        - printenv
