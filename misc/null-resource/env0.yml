version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - pwd
        - ls -la terraform.tfstate.d
        - ls -la .terraform
        - terraform workspace select $MY_CUSTOM_WORKSPACE

destroy:
  steps:
    terraformPlan:
      before:
        - terraform workspace select $MY_CUSTOM_WORKSPACE
