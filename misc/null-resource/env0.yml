version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - ls -la
        - terraform workspace select $MY_CUSTOM_WORKSPACE

destroy:
  steps:
    terraformPlan:
      before:
        - terraform workspace select $MY_CUSTOM_WORKSPACE
