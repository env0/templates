version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - terraform state list
        - terraform workspace select $MY_CUSTOM_WORKSPACE
        - terraform state list

destroy:
  steps:
    terraformPlan:
      before:
        - terraform workspace select $MY_CUSTOM_WORKSPACE
