version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - pwd
        - cat terraform.tfstate.d
        - ls -la
        - ls -la .terraform
        - terraform workspace select $MY_CUSTOM_WORKSPACE

destroy:
  steps:
    terraformPlan:
      before:
        - terraform workspace select $MY_CUSTOM_WORKSPACE
