version: 2
deploy:
  steps:
    terraformPlan:
      before:
      - name: tflint
        use: https://github.com/env0/templates
        inputs:
          version: v0.43.0
          directory: .
  onFailure:
    - name: tf-lint-on-success
      use: https://github.com/env0/templates
      inputs:
        version: v0.43.0
        directory: .

