environments:
  firstService:
    name: firstService
    templateName: 'null'
    workspace: firstService-${ENV0_DEPLOYMENT_LOG_ID}

  secondService:
    name: secondService
    templateName: 'null'
    workspace: secondService-${ENV0_DEPLOYMENT_LOG_ID}
    needs:
      - firstService
