environments:
  firstService:
    name: firstService
    templateName: 'null'
    workspace: 'firstService-${RANDOM}'

  secondService:
    name: secondService
    templateName: 'null'
    workspace: 'secondService-${RANDOM}'
    needs:
      - firstService
