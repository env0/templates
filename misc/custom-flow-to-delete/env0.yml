version: 2

deploy:
  steps:
    terraformPlan:
      after:
        - name: "checkov"
          run: "echo run checkov"
        - name: "send plan email (executeAfterChild)"
          run: "echo plan email after child"
          executeAfterChild: true
