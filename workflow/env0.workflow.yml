environments:
  service1:
    name: service1
    templateName: ${TEMPLATE}
    workspace: ${WORKSPACENAME}-1
    needs: []
  service2:
    name: service2
    templateName: ${TEMPLATE}
    workspace: ${WORKSPACENAME}-2
    needs: []
  service3:
    name: service3
    templateName: ${opt:TEMPLATE}
    workspace: ${WORKSPACENAME}-6
    needs: [service1, service2]
  service4:
    name: service4
    templateName: ${TEMPLATE}
    needs: []
  service5:
    name: service5
    templateName: ${TEMPLATE}
    needs: []
